
- test parallel

- b632+ test

- test_all anmachen

- listLearners with weights

- drop factor levels in makeTask
  schwierig. das ist beim subsetten des tasks?
  dann haben wir andere class levels? trat im allpairs exp auf.
auch doof:
task = makeClassifTask(data=iris, target="Species")
rin = makeFixedHoldoutInstance(train.inds=1:100, test.inds=101:150, size=150)
resample(makeLearner("classif.randomForest"), task, rin)
  
- rd name of supervisedtask, report roxygen2 bug
- examples
 
 - remove featperc measure? what to do?
   with chains it is diffuclt to see where feature number is

- desc object in task

es ist unklar wie wir die ver�nderten beschreibungen der daten abspeichern, wenn wir z.b.
per preproc alles potentiell ver�ndern.
a) ausgabe des modell mit print
b) wenn der task durchgereicht wird durch die trainingsfuns 
  
- add getLearnerModel, that we later can also add for BaseWrapper / ChainModel  
  
##### later ######  
  
- bbmisc function to print vector with names

- listMeasures 

- checkFeature names for predict

- bug in describe /staticdocs

- na.omit in mlrDataTask 
 
- allow task which only becomes true supervised task after preprocessing / feature
  extraction   

