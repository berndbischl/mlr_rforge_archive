test.weights <- function() {
	
#	m = train("regr.lm", task=regr.task)
#	pred = predict(m, newdata=regr.df)
#	p = performance
#	df = as.data.frame(p)

	#	cp = predict(cm5, task=ct5, subset=30:100)
#	cp["weights"] = 30:100
#	
#	df = as.data.frame(cp)
#	cn = colnames(df)
#	checkEquals(cn4, c("id", "response", "truth", "weights"))
	
	
}